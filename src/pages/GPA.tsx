import { Layout } from "@/components/Layout";
import { GPACalculator } from "@/components/GPACalculator";

const GPA = () => {
  return (
    <Layout>
      <GPACalculator />
    </Layout>
  );
};

export default GPA;